# Core dependencies
pyspark==3.4.1
pandas>=2.1.4
numpy>=1.26.0
flask>=2.3.0
requests>=2.28.0            # TMDB 电影海报 API
setuptools>=68.0           # Python 3.12+ distutils 兼容（PySpark 依赖）

# Dev / test
pytest>=8.0

# Optional: for HTML frontend CORS support
# flask-cors>=4.0.0

# Optional: for production WSGI server
# gunicorn>=21.2.0
